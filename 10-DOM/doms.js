/**
 * DOM, HTML etiketleri üzerinde gezinmeyi sağlar. 
 * Runtimeda dinamik olarak bir butona tıklandığında HTML üzerinde değişiklik yapmayı sağlar. Frontend kısmındadır.
 * Document, bütün html elemanlarının top kısmıdır, Document'in de bir üstü windowdur.
 * 
 * Hiyerarşi
 * Window -> Document -> Elements
 */

//console.log(window);

let val; 
console.log(document.location);

// val = document.location.href;
// val = document.location.hostname;
// val=document.location.port;
// val=document.location.pathname;
// val=document.characterSet;
// val=document.title;

// val=document.links.item(4); //elementi dogrudan yakalamak 
// val=document.links.item(4).getAttribute("id"); //id alanını aldık.
// val=document.links.item(4).classList;
// val=document.forms;
// val=document.forms.item(1).method; //formun metodunu alır.
// val=document.forms.item(1).id;
// val=document.forms.item(1).children;
// val=document.contentType;
 
console.log(val);